# 多题库目录 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 给现有装饰工程练习题库增加「题库目录」层级：现有题目归为「中级工程师」题库，新增「二建继续教育」题库（374题），进入 App 先选题库再练习，支持按章节筛选。

**Architecture:** 数据层由单一 `QUESTIONS` 数组改为 `BANKS` 数组（含题库元信息 + 题目）；存储层每条 record 新增 `bankId` 字段；视图层新增顶层 `bank-list`，`home`/`new-practice` 按当前题库渲染。启动时做一次性迁移，旧记录自动归到「中级工程师」。

**Tech Stack:** Vanilla HTML/CSS/JS (无框架), Python + openpyxl (数据导入), PWA (Service Worker)

---

## 文件结构

| 文件 | 类型 | 说明 |
|------|------|------|
| `scripts/excel_to_json.py` | 修改 | 改为双输入，生成 `BANKS` 格式 |
| `questions.js` | 重新生成 | 由脚本输出 `const BANKS = [...]` |
| `index.html` | 修改 | 新增 `bank-list` 视图，改造 `home` 头部和 `new-practice` 章节区 |
| `style.css` | 修改 | 新增题库卡片样式 |
| `app.js` | 修改 | 核心：题库目录、章节筛选、数据迁移 |
| `sw.js` | 修改 | 升 `CACHE_NAME` 到 `v3` |
| `manifest.json` | 修改 | 标题改为「工程师练习题库」 |

---

## Task 1: 修改 Excel 导入脚本

**Files:**
- Modify: `scripts/excel_to_json.py`

- [ ] **Step 1: 重写脚本，支持两个 Excel 输入**

  将脚本从单输入改为两个输入，输出 `BANKS` 格式。

  完整替换 `scripts/excel_to_json.py` 为：

  ```python
  #!/usr/bin/env python3
  # -*- coding: utf-8 -*-
  """
  Excel题库转JavaScript数据文件
  读取两个xlsx，输出 questions.js (BANKS 格式)
  """

  import json
  import re
  import sys
  from openpyxl import load_workbook

  BASE_DIR = r"F:/XJ/claude/工程师题库"
  OUTPUT_PATH = f"{BASE_DIR}/questions.js"

  def parse_options_pipe(question_type, options_text):
      """解析旧题库选项（按 | 分割）"""
      if question_type == "判断题":
          return [{"key": "正确", "text": "正确"}, {"key": "错误", "text": "错误"}]
      parts = [p.strip() for p in str(options_text or "").split("|") if p.strip()]
      options = []
      for part in parts:
          match = re.match(r'^([A-Z])[\.．、]\s*(.+)$', part)
          if match:
              options.append({"key": match.group(1), "text": match.group(2).strip()})
          else:
              options.append({"key": part[0] if part else "", "text": part})
      return options

  def parse_options_newline(question_type, options_text):
      """解析新题库选项（按 \n 分割）"""
      if question_type == "判断题":
          return [{"key": "正确", "text": "正确"}, {"key": "错误", "text": "错误"}]
      parts = [p.strip() for p in str(options_text or "").split("\n") if p.strip()]
      options = []
      for part in parts:
          match = re.match(r'^([A-Z])[\.．、]\s*(.+)$', part)
          if match:
              options.append({"key": match.group(1), "text": match.group(2).strip()})
          else:
              options.append({"key": part[0] if part else "", "text": part})
      return options

  def parse_bank_old():
      path = f"{BASE_DIR}/装饰工程网上学习题库.xlsx"
      wb = load_workbook(path, data_only=True)
      ws = wb.worksheets[0]
      questions = []
      for row in ws.iter_rows(min_row=3, values_only=True):
          if len(row) < 5:
              continue
          seq, q_type, title, options_text, answer = row[:5]
          if not title or not q_type:
              continue
          questions.append({
              "id": int(seq) if isinstance(seq, (int, float)) else len(questions) + 1,
              "type": str(q_type).strip(),
              "title": str(title).strip(),
              "options": parse_options_pipe(str(q_type).strip(), options_text),
              "answer": str(answer).strip() if answer else ""
          })
      return {
          "id": "mid-engineer",
          "name": "中级工程师",
          "hasChapters": False,
          "questions": questions
      }

  def parse_bank_new():
      path = f"{BASE_DIR}/课后习题汇总.xlsx"
      wb = load_workbook(path, data_only=True)
      ws = wb.worksheets[0]
      questions = []
      chapter_order = []
      for row in ws.iter_rows(min_row=2, values_only=True):
          if len(row) < 6:
              continue
          chapter, lesson, q_type, title, options_text, answer = row[:6]
          if not title or not q_type:
              continue
          chapter = str(chapter).strip()
          if chapter and chapter not in chapter_order:
              chapter_order.append(chapter)
          questions.append({
              "id": len(questions) + 1,
              "type": str(q_type).strip(),
              "title": str(title).strip(),
              "options": parse_options_newline(str(q_type).strip(), options_text),
              "answer": str(answer).strip() if answer else "",
              "chapter": chapter,
              "lesson": str(lesson).strip() if lesson else ""
          })
      return {
          "id": "erjian-edu",
          "name": "二建继续教育",
          "hasChapters": True,
          "chapters": chapter_order,
          "questions": questions
      }

  def main():
      old_bank = parse_bank_old()
      new_bank = parse_bank_new()
      banks = [old_bank, new_bank]

      js_content = "const BANKS = " + json.dumps(banks, ensure_ascii=False, indent=2) + ";\n"
      with open(OUTPUT_PATH, "w", encoding="utf-8") as f:
          f.write(js_content)

      for bank in banks:
          print(f"题库: {bank['name']} ({bank['id']})")
          if bank.get("hasChapters"):
              print(f"  章节: {len(bank['chapters'])}")
          type_counts = {}
          for q in bank["questions"]:
              type_counts[q["type"]] = type_counts.get(q["type"], 0) + 1
          for t, c in type_counts.items():
              print(f"  {t}: {c} 题")
          print(f"  总计: {len(bank['questions'])} 题")
      print(f"输出: {OUTPUT_PATH}")

  if __name__ == "__main__":
      main()
  ```

- [ ] **Step 2: 运行脚本生成 questions.js**

  ```bash
  cd "F:/XJ/claude/工程师题库/scripts"
  python excel_to_json.py
  ```

  期望输出：
  ```
  题库: 中级工程师 (mid-engineer)
    单选题: 97 题
    多选题: 10 题
    判断题: 34 题
    总计: 141 题
  题库: 二建继续教育 (erjian-edu)
    章节: 4
    单选题: 150 题
    多选题: 94 题
    判断题: 130 题
    总计: 374 题
  输出: F:/XJ/claude/工程师题库/questions.js
  ```

- [ ] **Step 3: 验证 questions.js 格式**

  ```bash
  cd "F:/XJ/claude/工程师题库"
  head -n 20 questions.js
  ```

  应看到 `const BANKS = [` 开头。

- [ ] **Step 4: Commit**

  ```bash
  git add scripts/excel_to_json.py questions.js
  git commit -m "$(cat <<'EOF'
  feat: regenerate questions.js with BANKS format, add erjian-edu bank

  Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
  EOF
  )"
  ```

---

## Task 2: 修改 index.html

**Files:**
- Modify: `index.html`

- [ ] **Step 1: 修改 `manifest.json` 标题（先在 head 里改 title）**

  改 `index.html:9`：
  ```html
  <title>工程师练习题库</title>
  ```

- [ ] **Step 2: 在 `<body>` 开头添加 `bank-list` 视图**

  在 `view-home` 之前插入：

  ```html
  <!-- 题库列表 -->
  <section id="view-bank-list" class="view active">
    <header class="app-header">
      <h1>工程师练习题库</h1>
    </header>
    <div class="container">
      <div id="bank-list" class="bank-list">
        <p class="empty-tip">暂无题库</p>
      </div>
    </div>
  </section>
  ```

  同时把 `view-home` 的 `class="view active"` 改为 `class="view"`（`active` 移到 bank-list）。

- [ ] **Step 3: 修改 `view-home` 头部**

  替换 `view-home` 的 header：
  ```html
  <header class="app-header">
    <button class="btn-back" data-target="view-bank-list">&#8249;</button>
    <h1 id="home-title">装饰工程练习题库</h1>
  </header>
  ```

- [ ] **Step 4: 在 `view-new-practice` 添加章节选择区**

  在「题目数量」表单组（`id="count-slider"` 所在的 div.form-group）之前插入：

  ```html
  <div class="form-group" id="chapter-group-wrapper" style="display:none;">
    <label class="form-label">选择章节</label>
    <div class="checkbox-group" id="chapter-group">
    </div>
  </div>
  ```

  注意：插入位置在「选择题型」之后，「题目数量」之前。

- [ ] **Step 5: Commit**

  ```bash
  git add index.html
  git commit -m "$(cat <<'EOF'
  feat: add bank-list view, chapter selector, update titles

  Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
  EOF
  )"
  ```

---

## Task 3: 修改 style.css

**Files:**
- Modify: `style.css`

- [ ] **Step 1: 在文件末尾添加题库卡片样式**

  追加到 `style.css` 末尾：

  ```css
  /* ===== Bank List ===== */
  .bank-list {
    display: flex;
    flex-direction: column;
    gap: 12px;
  }

  .bank-item {
    background: #fff;
    border-radius: 12px;
    padding: 16px;
    display: flex;
    align-items: center;
    justify-content: space-between;
    cursor: pointer;
    transition: background 0.2s;
    box-shadow: 0 1px 4px rgba(0, 0, 0, 0.04);
  }

  .bank-item:active {
    background: #f8f8f8;
  }

  .bank-name {
    font-size: 16px;
    font-weight: 600;
    color: #1a1a1a;
    margin-bottom: 4px;
  }

  .bank-count {
    font-size: 13px;
    color: #999;
  }

  .bank-arrow {
    font-size: 20px;
    color: #999;
    font-weight: 300;
  }
  ```

- [ ] **Step 2: Commit**

  ```bash
  git add style.css
  git commit -m "$(cat <<'EOF'
  feat: add bank-list card styles

  Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
  EOF
  )"
  ```

---

## Task 4: 修改 app.js - 题库列表与导航

**Files:**
- Modify: `app.js`

- [ ] **Step 1: 修改全局变量**

  在 `app.js:4-8`（Global State 区域），在 `lastViewId` 下面添加：

  ```js
  let currentBankId = null;
  ```

  变成：
  ```js
  // ===== Global State =====
  let currentPractice = null;
  let currentQuestionIndex = 0;
  let hasSubmitted = false;
  let lastViewId = 'home';
  let currentBankId = null;
  ```

- [ ] **Step 2: 添加数据迁移函数**

  在 Storage Layer 后面（`app.js:49` 之后，Utility Functions 之前）插入：

  ```js
  // ===== Data Migration =====
  function migrateRecords() {
    const records = loadRecords();
    let dirty = false;
    for (const r of records) {
      if (!r.bankId) {
        r.bankId = 'mid-engineer';
        dirty = true;
      }
    }
    if (dirty) saveRecords(records);
  }
  ```

- [ ] **Step 3: 添加题库相关工具函数**

  在 Utility Functions 区域（`app.js:71` 之后，Practice Builder 之前）插入：

  ```js
  function getCurrentBank() {
    return BANKS.find(b => b.id === currentBankId) || null;
  }
  ```

- [ ] **Step 4: 添加题库列表渲染和导航函数**

  在 `renderHome()` 之前（`app.js:176` 之前）插入：

  ```js
  // ===== Bank List View =====
  function renderBankList() {
    const listEl = document.getElementById('bank-list');
    if (!BANKS || !BANKS.length) {
      listEl.innerHTML = '<p class="empty-tip">暂无题库</p>';
      return;
    }
    listEl.innerHTML = BANKS.map(b => `
      <div class="bank-item" data-id="${b.id}">
        <div class="bank-info">
          <div class="bank-name">📚 ${b.name}</div>
          <div class="bank-count">${b.questions.length} 题</div>
        </div>
        <div class="bank-arrow">›</div>
      </div>
    `).join('');
    listEl.querySelectorAll('.bank-item').forEach(el => {
      el.addEventListener('click', () => enterBank(el.dataset.id));
    });
  }

  function enterBank(bankId) {
    currentBankId = bankId;
    showView('home');
    renderHome();
  }
  ```

- [ ] **Step 5: 修改 `renderHome()`**

  替换 `renderHome()` 的前 5 行（`app.js:176-180`）：

  原代码：
  ```js
  function renderHome() {
    const records = loadRecords();
    const listEl = document.getElementById('history-list');
  ```

  改为：
  ```js
  function renderHome() {
    const bank = getCurrentBank();
    if (bank) {
      document.getElementById('home-title').textContent = bank.name;
    }
    const records = loadRecords().filter(r => r.bankId === currentBankId);
    const listEl = document.getElementById('history-list');
  ```

- [ ] **Step 6: Commit**

  ```bash
  git add app.js
  git commit -m "$(cat <<'EOF'
  feat: bank list rendering, navigation, record migration

  Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
  EOF
  )"
  ```

---

## Task 5: 修改 app.js - 章节筛选逻辑

**Files:**
- Modify: `app.js`

- [ ] **Step 1: 修改 `buildPractice()`**

  替换 `buildPractice()`（`app.js:114-121`）：

  原代码：
  ```js
  function buildPractice(allQuestions, config) {
    const { types, count, random } = config;
    let filtered = allQuestions.filter(q => types.includes(q.type));
    if (random) {
      filtered = shuffleArray(filtered);
    }
    return filtered.slice(0, count);
  }
  ```

  改为：
  ```js
  function buildPractice(bank, config) {
    const { types, count, random, chapters } = config;
    let filtered = bank.questions.filter(q => types.includes(q.type));
    if (bank.hasChapters && chapters && chapters.length > 0) {
      filtered = filtered.filter(q => chapters.includes(q.chapter));
    }
    if (random) {
      filtered = shuffleArray(filtered);
    }
    return filtered.slice(0, count);
  }
  ```

- [ ] **Step 2: 修改 `createPractice()`**

  替换 `createPractice()`（`app.js:123-139`）：

  原代码：
  ```js
  function createPractice(config) {
    const questions = buildPractice(QUESTIONS, config);
    return {
      id: String(Date.now()),
      createdAt: nowString(),
      finishedAt: null,
      status: 'in_progress',
      config: { ...config },
      source: 'new',
      sourcePracticeId: null,
      questions,
      userAnswers: {},
      wrongList: [],
      totalCount: questions.length,
      wrongCount: 0
    };
  }
  ```

  改为：
  ```js
  function createPractice(config) {
    const bank = getCurrentBank();
    const questions = buildPractice(bank, config);
    return {
      id: String(Date.now()),
      bankId: currentBankId,
      createdAt: nowString(),
      finishedAt: null,
      status: 'in_progress',
      config: { ...config },
      source: 'new',
      sourcePracticeId: null,
      questions,
      userAnswers: {},
      wrongList: [],
      totalCount: questions.length,
      wrongCount: 0
    };
  }
  ```

- [ ] **Step 3: 修改 `createReviewPractice()`**

  替换 `createReviewPractice()`（`app.js:141-158`）：

  原代码：
  ```js
  function createReviewPractice(sourceRecord) {
    const wrongIds = getWrongList(sourceRecord);
    const questions = sourceRecord.questions.filter(q => wrongIds.includes(q.id));
    return {
      id: String(Date.now()),
      createdAt: nowString(),
      finishedAt: null,
      status: 'in_progress',
      config: { ...sourceRecord.config },
      source: 'review',
      sourcePracticeId: sourceRecord.id,
      questions,
      userAnswers: {},
      wrongList: [],
      totalCount: questions.length,
      wrongCount: 0
    };
  }
  ```

  改为：
  ```js
  function createReviewPractice(sourceRecord) {
    const wrongIds = getWrongList(sourceRecord);
    const questions = sourceRecord.questions.filter(q => wrongIds.includes(q.id));
    return {
      id: String(Date.now()),
      bankId: sourceRecord.bankId,
      createdAt: nowString(),
      finishedAt: null,
      status: 'in_progress',
      config: { ...sourceRecord.config },
      source: 'review',
      sourcePracticeId: sourceRecord.id,
      questions,
      userAnswers: {},
      wrongList: [],
      totalCount: questions.length,
      wrongCount: 0
    };
  }
  ```

- [ ] **Step 4: 重写 `updateNewPracticeUI()`**

  替换 `updateNewPracticeUI()`（`app.js:499-514`）：

  原代码：
  ```js
  function updateNewPracticeUI() {
    const typeCounts = {};
    QUESTIONS.forEach(q => {
      typeCounts[q.type] = (typeCounts[q.type] || 0) + 1;
    });

    const badges = document.querySelectorAll('#type-group .count-badge');
    const typeOrder = ['单选题', '多选题', '判断题'];
    badges.forEach((badge, i) => {
      const type = typeOrder[i];
      if (type) {
        badge.textContent = typeCounts[type] || 0;
      }
    });
  }
  ```

  改为：
  ```js
  function updateNewPracticeUI() {
    const bank = getCurrentBank();
    const questions = bank ? bank.questions : [];
    const typeCounts = {};
    questions.forEach(q => {
      typeCounts[q.type] = (typeCounts[q.type] || 0) + 1;
    });

    const badges = document.querySelectorAll('#type-group .count-badge');
    const typeOrder = ['单选题', '多选题', '判断题'];
    badges.forEach((badge, i) => {
      const type = typeOrder[i];
      if (type) {
        badge.textContent = typeCounts[type] || 0;
      }
    });

    updateChapterUI();
  }
  ```

- [ ] **Step 5: 添加章节 UI 相关函数**

  在 `updateNewPracticeUI()` 后面（`app.js:514` 之后）插入：

  ```js
  function updateChapterUI() {
    const bank = getCurrentBank();
    const wrapper = document.getElementById('chapter-group-wrapper');
    const group = document.getElementById('chapter-group');

    if (!bank || !bank.hasChapters) {
      wrapper.style.display = 'none';
      group.innerHTML = '';
      return;
    }

    wrapper.style.display = '';
    group.innerHTML = bank.chapters.map((ch, i) => `
      <label class="checkbox-item">
        <input type="checkbox" value="${ch}" checked>
        <span class="checkbox-box"></span>
        <span class="checkbox-text">${ch}</span>
      </label>
    `).join('');

    // 绑定 change 事件到 max count 更新
    group.querySelectorAll('input[type="checkbox"]').forEach(cb => {
      cb.addEventListener('change', updateMaxCount);
    });
  }

  function getSelectedChapters() {
    const checkboxes = document.querySelectorAll('#chapter-group input[type="checkbox"]:checked');
    return Array.from(checkboxes).map(cb => cb.value);
  }
  ```

- [ ] **Step 6: 修改 `getMaxCount()`**

  替换 `getMaxCount()`（`app.js:521-525`）：

  原代码：
  ```js
  function getMaxCount() {
    const types = getSelectedTypes();
    if (!types.length) return 0;
    return QUESTIONS.filter(q => types.includes(q.type)).length;
  }
  ```

  改为：
  ```js
  function getMaxCount() {
    const types = getSelectedTypes();
    if (!types.length) return 0;
    const bank = getCurrentBank();
    if (!bank) return 0;
    let filtered = bank.questions.filter(q => types.includes(q.type));
    if (bank.hasChapters) {
      const chapters = getSelectedChapters();
      if (chapters.length === 0) return 0;
      filtered = filtered.filter(q => chapters.includes(q.chapter));
    }
    return filtered.length;
  }
  ```

- [ ] **Step 7: Commit**

  ```bash
  git add app.js
  git commit -m "$(cat <<'EOF'
  feat: chapter filtering, bank-scoped practice builder

  Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
  EOF
  )"
  ```

---

## Task 6: 修改 app.js - 事件绑定和启动

**Files:**
- Modify: `app.js`

- [ ] **Step 1: 修改 `btn-new-practice` 点击事件**

  替换 `btnNewPractice` 的点击事件处理（`app.js:556-561`）：

  原代码：
  ```js
  btnNewPractice.addEventListener('click', () => {
    showView('new-practice');
    updateNewPracticeUI();
  });
  ```

  改为：
  ```js
  btnNewPractice.addEventListener('click', () => {
    showView('new-practice');
    updateNewPracticeUI();
    updateChapterUI();
    updateMaxCount();
  });
  ```

- [ ] **Step 2: 修改 `btn-start` 点击事件**

  替换 `btnStart` 的点击事件处理（`app.js:601-619`）：

  原代码：
  ```js
  btnStart.addEventListener('click', () => {
    const types = getSelectedTypes();
    if (types.length === 0) {
      alert('请至少选择一种题型');
      return;
    }
    const countInput = document.getElementById('count-input');
    const shuffleToggle = document.getElementById('shuffle-toggle');
    const config = {
      types,
      count: parseInt(countInput.value, 10),
      random: shuffleToggle.checked
    };
    const record = createPractice(config);
    saveRecord(record);
    startPractice(record);
  });
  ```

  改为：
  ```js
  btnStart.addEventListener('click', () => {
    const types = getSelectedTypes();
    if (types.length === 0) {
      alert('请至少选择一种题型');
      return;
    }
    const bank = getCurrentBank();
    let chapters = null;
    if (bank && bank.hasChapters) {
      chapters = getSelectedChapters();
      if (chapters.length === 0) {
        alert('请至少选择一个章节');
        return;
      }
    }
    const countInput = document.getElementById('count-input');
    const shuffleToggle = document.getElementById('shuffle-toggle');
    const config = {
      types,
      count: parseInt(countInput.value, 10),
      random: shuffleToggle.checked,
      chapters
    };
    const record = createPractice(config);
    saveRecord(record);
    startPractice(record);
  });
  ```

- [ ] **Step 3: 修改 `DOMContentLoaded` 初始化**

  替换初始化代码末尾（`app.js:660-662`）：

  原代码：
  ```js
  // Initialize
  renderHome();
  ```

  改为：
  ```js
  // Initialize
  migrateRecords();
  renderBankList();
  ```

- [ ] **Step 4: Commit**

  ```bash
  git add app.js
  git commit -m "$(cat <<'EOF'
  feat: wire up chapter selection, bank-scoped start button, init flow

  Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
  EOF
  )"
  ```

---

## Task 7: 修改 sw.js 和 manifest.json

**Files:**
- Modify: `sw.js`
- Modify: `manifest.json`

- [ ] **Step 1: 升级 Service Worker 缓存版本**

  改 `sw.js:1`：
  ```js
  const CACHE_NAME = 'deco-quiz-v3';
  ```

- [ ] **Step 2: 更新 manifest.json 标题**

  替换 `manifest.json` 内容：
  ```json
  {
    "name": "工程师练习题库",
    "short_name": "工程师题库",
    "start_url": "./index.html",
    "display": "standalone",
    "background_color": "#f5f5f5",
    "theme_color": "#1677ff",
    "orientation": "portrait",
    "icons": []
  }
  ```

- [ ] **Step 3: Commit**

  ```bash
  git add sw.js manifest.json
  git commit -m "$(cat <<'EOF'
  feat: bump SW cache to v3, update app name in manifest

  Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
  EOF
  )"
  ```

---

## Task 8: 浏览器端到端验证

**Files:**
- 无需修改文件，仅在浏览器中验证

- [ ] **Step 1: 本地启动 HTTP 服务器**

  ```bash
  cd "F:/XJ/claude/工程师题库"
  python -m http.server 8080
  ```

  在另一个终端打开浏览器访问 `http://localhost:8080`。

- [ ] **Step 2: 验证题库列表页**

  打开页面后应看到：
  - 标题「工程师练习题库」
  - 两个题库卡片：「中级工程师 141 题」和「二建继续教育 374 题」
  - 无「暂无题库」提示

- [ ] **Step 3: 验证中级工程师题库**

  点击「中级工程师」进入主页：
  - 标题变为「中级工程师」
  - 头部有返回箭头，点击返回题库列表
  - 如果有旧历史记录，应能看到（已迁移）
  - 点击「新建练习」→ **不应**出现「选择章节」区域
  - 题型计数正确（单选 97 / 多选 10 / 判断 34）
  - 最大题数 = 141
  - 开始练习 → 答题 → 提交 → 下一题 → 完成 → 结果页 → 错题回顾，流程闭环

- [ ] **Step 4: 验证二建继续教育题库**

  返回题库列表，点击「二建继续教育」：
  - 标题变为「二建继续教育」
  - 历史记录为空（或只有本次的）
  - 点击「新建练习」→ **应出现**「选择章节」区域，4 个章节默认全勾
  - 题型计数正确（单选 150 / 多选 94 / 判断 130）
  - 最大题数 = 374
  - 取消勾选「第1章」，最大题数应减少
  - 取消所有章节，点击「开始练习」应弹窗「请至少选择一个章节」
  - 只勾「第1章」+ 只勾「判断题」，最大题数 = 第1章判断题数
  - 开始练习 → 答题 → 完成 → 结果页 → 错题回顾 → 重练错题，流程闭环

- [ ] **Step 5: 验证题库隔离**

  - 在两个题库分别创建练习记录
  - 返回题库列表切换题库
  - 确认每个题库的历史记录互不干扰
  - 删除一个题库的记录不影响另一个

- [ ] **Step 6: 验证 PWA 离线**

  - 刷新页面让 Service Worker 更新到 v3
  - 打开 DevTools → Application → Service Workers，确认是 v3
  - 断网，刷新页面，应仍能正常访问（题库列表页）

- [ ] **Step 7: 最终 commit**

  如果验证全部通过：
  ```bash
  git log --oneline -10
  ```

  确认所有 commit 都在，最终状态 clean。

---

## Self-Review Checklist

**1. Spec coverage：**
- [x] 题库列表页 → Task 2 + Task 4
- [x] 中级工程师题库归并 → Task 1 + Task 4 (migrateRecords)
- [x] 二建继续教育导入 → Task 1
- [x] 按章节筛选 → Task 5 + Task 6
- [x] 历史记录按题库隔离 → Task 4 (renderHome filter)
- [x] 旧记录迁移 → Task 4 (migrateRecords)
- [x] PWA 缓存更新 → Task 7

**2. Placeholder scan：** 无 TBD/TODO/"implement later"。

**3. Type consistency：**
- `bankId` 字段在所有 record 中存在
- `config.chapters` 在 createPractice 中正确传递
- `BANKS` 变量名在所有读取点一致
- `hasChapters` 布尔值判断一致
